 applications:
 - name: challenge-ibm-frontend
   path: ./dist
   buildpack: https://github.com/cloudfoundry/staticfile-buildpack.git
   random-route: true
   memory: 512M
   env:
   FORCE_HTTPS: true